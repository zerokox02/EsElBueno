specVersion: "v1"
serviceId: "assets"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "assets-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
